#FS (file system)
It  direct connet with client OS rater than browser.
## Major task of FS Module
-Reading and Writing Files.
  -readFile()
  -writeFile()
  -appendFile()
-Directiry Management
  -mkdir()
  -rmdir()
  -readdir() -depricreated
  -rm()
-Metadata/Information
  -stat()
  -lstat()
  -fstat()
-Watching For changes
  -watch()
  -watchFile()
  -unwatchFile()
streaming large file
  -createReadStream()
  -createWriteStream()
-File Operations
  -rename()
  -truncate()
  -unlike()
  -link()
  -syslink()


C=Create/add/insert.
R=retrive/view/get.
U=update/edit
D=delete/remove

## CURD
create/Insert,Read/REtrieve,update,Delete
## Each item
(id,name,prize,qty)

## operations
1.add to cart
2.show cart
3.remove item from cart.
update quantity from cart.
5.checkout.
NOTE:- all item will be stored in add,so after termination of program we can retrive cart details.


## required file
1.crud.js-it contains all the methods and entry point
2.products.json-it contains the product details in array from.


